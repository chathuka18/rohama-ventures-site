import { Link } from 'react-router-dom'
import { ArrowUpRight } from 'lucide-react'
import PageHero from '../components/PageHero'
import CTA from '../components/CTA'
import { services } from '../data/content'
export default function Services(){return <><PageHero eyebrow="Services" title="A full-spectrum business & financial advisory platform." body="Our services are organised around five strategic pillars spanning the lifecycle of a business and financial decision — from transaction through restructuring to long-term planning."/><section className="bg-white py-20"><div className="container-shell space-y-5">{services.map(s=><Link to={`/services/${s.slug}`} key={s.slug} className="group grid gap-5 rounded-[2rem] border border-navy-900/10 bg-mist p-6 transition hover:bg-navy-900 hover:text-white sm:grid-cols-[100px_1fr_auto] sm:items-center sm:p-8"><div className="font-display text-5xl text-gold-500">{s.number}</div><div><h2 className="text-xl font-semibold">{s.title}</h2><p className="mt-2 max-w-2xl text-sm leading-6 opacity-55">{s.short}</p><div className="mt-4 flex flex-wrap gap-2">{s.items.map(x=><span className="rounded-full border border-current/15 px-3 py-1.5 text-xs opacity-70" key={x}>{x}</span>)}</div></div><ArrowUpRight className="transition group-hover:rotate-45"/></Link>)}</div></section><CTA/></>}
