# ROHAMA Ventures Website — Images Only Update

This version preserves the existing website content and wording from the previous ROHAMA website version.

The update only adds the supplied visual assets to the relevant pages and uses the supplied ROHAMA logo.

## Run locally

```bash
npm install
npm run dev
```

Then open the Vite local URL, usually `http://localhost:5173`.
